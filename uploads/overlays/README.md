# Uploads Directory

This directory stores user-uploaded files.

## Purpose
- User-uploaded overlay images are stored in the `overlays/` subdirectory
- The main uploads directory structure is tracked in git to ensure it exists on all platforms
- Uploaded files and subdirectories are ignored by git (see .gitignore)